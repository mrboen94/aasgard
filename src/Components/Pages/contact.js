import React from "react";
import styled from "styled-components";

const StyledDiv = styled.div`
  background-color: white;
  padding: 1em;
`;

export default function HomeBody() {
  return (
    <StyledDiv>
      <p>Here I will list my contact information when I get there.</p>
    </StyledDiv>
  );
}
